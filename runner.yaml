runner:
  name: Example Creds Runner
  description: This runner is testing out requiring the OPEN_API_KEY and SECRET_KEY user creds
  version: 0.0.2

creds:
  - OPEN_API_KEY
  - SECRET_KEY

launch:
  command: app.py
